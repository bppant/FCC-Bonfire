function end(str, target) {
  // "Never give up and good luck will find you."
  // -- Falcor
  
  var tarLen= target.length;
  var strLen= str.length;
           
    
  if(str.substr(strLen-tarLen)==target) {
    return true;
  }
  
  else {
    
    return false;
  }
  
}

end("Bastian", "n");
